name: CI
# Traceability:
# - APP_MASTER_SPEC.md#10-testing-strategy
# - APP_MASTER_SPEC.md#11-cicd--deployment
# - APP_MASTER_SPEC.md#13-release-verification-gate-all-yes-required
# - TRACEABILITY_MAP.md

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck (if script exists)
        run: npm run typecheck --if-present

      - name: Lint (if script exists)
        run: npm run lint --if-present

      - name: Unit/Integration tests (if script exists)
        run: npm run test --if-present

      - name: Build
        run: npm run build

      - name: Upload build artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: web-dist
          path: dist
          if-no-files-found: ignore

  e2e-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: build-test
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers (if needed)
        run: npx playwright install --with-deps
        continue-on-error: true

      - name: E2E tests (if script exists)
        run: npm run test:e2e --if-present

  migration-sanity:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate migration filename pattern
        run: |
          FAIL=0
          if [ -d "supabase/migrations" ]; then
            for file in supabase/migrations/*.sql; do
              base=$(basename "$file")
              if ! echo "$base" | grep -Eq '^[0-9]{14}_.+\.sql$'; then
                echo "INVALID migration filename: $base"
                FAIL=1
              fi
            done
          fi
          exit $FAIL

      - name: Verify no local API hardcodes in src
        run: |
          if grep -rn "http://localhost:[0-9]\+\|ws://localhost:[0-9]\+" src/; then
            echo "Found forbidden localhost hardcoded API/WS URLs in src"
            exit 1
          fi
          echo "No localhost hardcodes found"

      - name: Run Database Tests
        run: npx supabase db lint || echo "supabase db lint skipped or unavailable"
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
        continue-on-error: true
